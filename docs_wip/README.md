# Documentation Reset

The documentation has been restarted from a clean Sphinx root.

- Legacy content is preserved in `docs_legacy/` for migration.
- The active Sphinx config is `docs/conf.py`.
- Content will be rebuilt and moved into this `docs/` tree over time.
